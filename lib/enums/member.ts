export enum MemberType {
  USER = "USER",
  AGENT = "AGENT",
  ADMIN = "ADMIN",
}

export enum MemberStatus {
  ACTIVE = "ACTIVE",
  BLOCKED = "BLOCKED",
  DELETE = "DELETE",
}

export enum MemberAuthType {
  PHONE = "PHONE",
  EMAIL = "EMAIL",
  TELEGRAM = "TELEGRAM",
}

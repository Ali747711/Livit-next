// Layout is now handled centrally in pages/_app.tsx via AppShell.
// This stub exists so any residual imports don't break at build time.
const Layout = (Component: any) => Component;
export default Layout;
